# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=postgres
DB_SYNCHRONIZE=true

# Tags Database (separate connection for parser tags)
TAGS_DB_HOST=localhost
TAGS_DB_PORT=5432
TAGS_DB_USERNAME=postgres
TAGS_DB_PASSWORD=postgres
TAGS_DB_DATABASE=postgres
TAGS_DB_SYNCHRONIZE=true

# Server Configuration
PORT=3001

# Alternative: Use DATABASE_URL instead of individual DB settings
# DATABASE_URL=postgresql://username:password@host:port/database
# DB_SSL=true
